import { PlaceholderPage } from "@/components/PlaceholderPage";

const Workflows = () => (
  <PlaceholderPage
    title="Workflows"
    phase="Phase 5"
    description="Workflow builder, triggers, and execution logs."
  />
);
export default Workflows;
